#include <iostream>
#include <cassert>
#include "../timer/MyHeap.h"

int main() {
    // // 创建一个小根堆
    // MyHeap<int> hh;

    // // 添加元素
    // hh.add(3);
    // hh.add(1);
    // hh.add(4);
    // hh.add(1);
    // hh.add(5);

    // for (int i = 0; i < hh.heap.size(); ++i) {
    //     std::cout << hh.heap[i] << std::endl;
    // }   
    // std::cout << std::endl;

    // // 测试堆的大小
    // assert(hh.heap.size() == 5);

    // // 测试堆的顶部元素
    // assert(hh.heap[0] == 1);

    // // 删除元素
    // hh.del(1);

    // // 测试堆的大小
    // assert(hh.heap.size() == 4);

    // // 测试堆的顶部元素
    // assert(hh.heap[0] == 1);

    // // 弹出元素
    // hh.pop();

    // // 测试堆的大小
    // assert(hh.heap.size() == 3);

    // // 测试堆的顶部元素
    // assert(hh.heap[0] == 3);

    // // 测试堆元素修改
    // hh.getEle(3) = 6;
    // hh.adjust();

    // for (int i = 0; i < hh.heap.size(); ++i) {
    //     std::cout << hh.heap[i] << std::endl;
    // }

    // return 0;
}